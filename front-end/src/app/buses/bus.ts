export interface busCreacionDTO {
    tipoBus: string,
    capBus: number,
    costoAsiento: number
}

export interface busDTO{
    id: number,
    tipoBus: string,
    capBus: number,
    costoAsiento: number
}

export interface busTicketDTO{
    id: number,
    tipoBus: string,
    capBus: number,
    costoAsiento: number
}